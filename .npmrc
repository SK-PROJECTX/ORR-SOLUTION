# Approve build scripts in CI for native modules (sharp, unrs-resolver)
# WARNING: allowing scripts can be a security risk for untrusted dependencies.
# Use only if you trust your dependencies and repository.
allow-scripts=true
